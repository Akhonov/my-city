# Digital Twin City Backend (Core Setup)

This is the minimal core setup for the Digital Twin City backend.

It includes:
- FastAPI app
- Configuration system (.env support)
- Database setup (SQLAlchemy + SQLite)
- Automatic DB initialization
- Seed logic placeholder

## Setup

### 1. Create virtual environment
```bash
python -m venv .venv
source .venv/bin/activate